這是一種用於管理關聯式資料庫系統（RDBMS）的標準化語言。

主要用來對資料庫進行查詢、新增、更新與刪除（CRUD）操作，是所有後端開發者與資料分析師必備的核心技能。

> **🍿 比喻單元**：
> 如果資料庫是一個**擁有無數抽屜的超大檔案櫃**；
> 那麼 SQL 就是你對圖書館管理員下的**精確拿取指令**！

---

## 核心概念

SQL 語言主要可以分為幾大類：
- **DQL (資料查詢語言)**：用來查詢資料（如 `SELECT`）。
- **DML (資料操作語言)**：用來異動資料（如 `INSERT`, `UPDATE`, `DELETE`）。
- **DDL (資料定義語言)**：用來定義資料表結構（如 `CREATE`, `ALTER`, `DROP`）。

---

## 基礎查詢 (DQL)

最常見的操作，用於從資料表中撈取所需的資料。

##### SELECT 查詢資料

- **使用時機**：當你需要從資料表中挑選特定的欄位，或查看整張表的內容時使用。
- **語法**：`SELECT 欄位名稱 FROM 資料表名稱`
- **參數說明**：
  - `欄位名稱`：想看的欄位（使用 `*` 代表全選所有欄位）。
  - `資料表名稱`：目標資料表。
- **回傳值**：回傳符合條件的資料集（Result Set）。

```sql
-- 1. 專注展示選取全部欄位
SELECT * FROM users;

-- 2. 專注展示只選取特定欄位
SELECT id, name, email FROM users;
```

> **💡 小提醒**：在正式環境的伺服器中，請盡量避免使用 `SELECT *`，這會造成資料庫極大的負擔與網路頻寬浪費！請明確寫出需要的欄位名稱。

---

##### WHERE 條件過濾

- **使用時機**：當你只需要找出符合特定條件的資料（例如：年齡大於 18 歲的用戶）時。
- **語法**：`SELECT ... FROM ... WHERE 條件式`
- **參數說明**：
  - `條件式`：可使用 `=`, `>`, `<`, `!=`, `AND`, `OR` 等邏輯運算子。
- **回傳值**：過濾後剩下的資料集。

```sql
-- 專注展示多條件過濾
SELECT name, age 
FROM users 
WHERE age >= 18 AND status = 'active';
```

---

##### ORDER BY 排序結果

- **使用時機**：需要將撈出來的資料照順序排列（如價格從高到低、註冊時間從新到舊）時。
- **語法**：`ORDER BY 欄位名稱 [ASC | DESC]`
- **參數說明**：
  - `ASC`：預設值，由小到大（遞增）。
  - `DESC`：由大到小（遞減）。
- **回傳值**：已排好序的資料集。

```sql
-- 專注展示依據建立時間進行遞減排序
SELECT name, created_at 
FROM users 
ORDER BY created_at DESC;
```

---

## 資料操作 (DML)

用於對資料表內的紀錄進行新增、修改與刪除。

##### INSERT INTO 新增資料

- **使用時機**：當有新用戶註冊、新訂單成立，需要將資料寫入資料庫時。
- **語法**：`INSERT INTO 資料表 (欄位1, 欄位2) VALUES (值1, 值2)`
- **參數說明**：
  - `欄位`：要插入目標的欄位名稱。
  - `值`：必須與欄位的順序、資料型別一一對應。字串需用單引號 `'` 包覆。
- **回傳值**：資料庫受影響的行數。

```sql
-- 專注展示新增單筆資料
INSERT INTO users (name, email, age) 
VALUES ('Matthew', 'matt@example.com', 25);
```

---

##### UPDATE 修改資料

- **使用時機**：使用者想更改密碼、更新個人檔案，或是更改商品價格時。
- **語法**：`UPDATE 資料表 SET 欄位 = 新值 WHERE 條件式`
- **參數說明**：
  - `SET`：指定要更新的欄位與其新值。
  - `WHERE`：指定要更新哪些資料（極度重要！）。
- **回傳值**：資料庫受影響的行數。

```sql
-- 專注展示更新特定用戶的狀態
UPDATE users 
SET status = 'inactive' 
WHERE id = 42;
```

> **💡 小提醒**：執行 `UPDATE` 時**絕對、千萬要記得加上 `WHERE` 條件**！否則你會不小心把整張表的所有資料全部改成同一個值，造成毀滅性的災難！

---

##### DELETE 刪除資料

- **使用時機**：需要將資料從資料庫中永久抹除時。
- **語法**：`DELETE FROM 資料表 WHERE 條件式`
- **參數說明**：
  - `WHERE`：指定要刪除哪些資料。
- **回傳值**：資料庫受影響的行數。

```sql
-- 專注展示刪除特定資料
DELETE FROM users 
WHERE id = 42;
```

> **💡 小提醒**：跟 `UPDATE` 一樣，沒有加上 `WHERE` 的 `DELETE` 會清空整張表！在實務中，我們通常不會真的刪除資料，而是使用「軟刪除 (Soft Delete)」，也就是用 `UPDATE` 把一個 `is_deleted` 欄位設為 `True`。

---
