# 概念與原理

## Vim 核心哲學：模態編輯與語言化操作

Vim 是一套基於**模態切換 (Modal Editing)** 的純鍵盤高效文字編輯體系

在傳統編輯器中，鍵盤輸入直接等同於文字寫入，游標移動與文字編輯通常依賴滑鼠或複合修飾鍵（如 `Ctrl`, `Alt`, `Cmd`）

Vim 改變了這一邏輯，將編輯行為拆分為不同的「模式」

在最核心的 **Normal 模式** 下，鍵盤上的每一個英文字母都是一組指令

Vim 的操作指令具有高度結構化的**「動詞 + 介系詞/修飾詞 + 名詞」**文法特性：

$$\text{操作 (Operator)} + \text{範圍/數量 (Count)} + \text{目標物件 (Motion / Text Object)}$$

例如：`d` (Delete 刪除) + `i` (Inner 內部) + `w` (Word 單詞) = `diw`（刪除目前單詞）

> **比喻單元**：  
> 傳統編輯器就像拿著一支永遠處於書寫狀態的鋼筆，要翻頁或塗改時必須放下筆、拿起橡皮擦或換手翻頁  
> Vim 則像手握一把多功能手術刀，預設處於精準操作狀態（Normal Mode），只有在明確發出「開始書寫」指令時才切換為筆尖（Insert Mode）

---

## VS Code Vim 插件定位

VS Code Vim 插件（`vscodevim.vim`）是 VS Code 生態中最流行的鍵位模擬擴充套件

它不是在 VS Code 內部完整跑一套獨立的 Vim 直譯器，而是**在 VS Code 的編輯器核心上，模擬 Vim 的鍵位邏輯與模態行為**

這帶來了兩大核心優勢：
1. **保留 VS Code 現代 IDE 能力**：原生語言伺服器 (LSP)、程式碼自動補全 (IntelliSense)、偵錯工具、Git 整合與外掛生態完全不受影響
2. **無縫繼承 Vim 極速編輯流**：完全免除手腕頻繁移動至滑鼠或方向鍵的延遲，達到盲打編輯境界

---

# 核心功能與指令字典

| 類別 | 指令 / 鍵位 | 語法與說明 |
| :--- | :--- | :--- |
| **模式切換** | [[#i／I 進入插入模式\|i / I]] | 在游標前 / 當前行首進入 Insert 模式 |
| **模式切換** | [[#a／A 進入插入模式\|a / A]] | 在游標後 / 當前行尾進入 Insert 模式 |
| **模式切換** | [[#o／O 新增行進入插入模式\|o / O]] | 在下方 / 上方新開一行並進入 Insert 模式 |
| **模式切換** | [[#Esc／Ctrl+[ 返回一般模式\|Esc / Ctrl+\[]] | 退出當前模式，返回 Normal 模式 |
| **模式切換** | [[#v／V／Ctrl+v 可視模式\|v / V / Ctrl+v]] | 進入字元 / 整行 / 區塊可視選取模式 |
| **游標移動** | [[#h／j／k／l 基礎方向導航\|h / j / k / l]] | 左 / 下 / 上 / 右 移動一個字元 |
| **游標移動** | [[#w／b／e 單詞導航\|w / b / e]] | 移動至下一個單詞開頭 / 前一個單詞開頭 / 當前單詞結尾 |
| **游標移動** | [[#0／^／$ 行內邊界跳轉\|0 / ^ / $]] | 跳轉至行首 / 行首首個非空白字元 / 行尾 |
| **游標移動** | [[#f／F／t／T 行內字元精準跳轉\|f / F / t / T]] | 行內向後/向前搜尋並跳轉至/停留於指定字元 |
| **游標移動** | [[#gg／G／行號跳轉 檔案全文跳轉\|gg / G / :[行號]]] | 跳轉至檔案首行 / 檔案尾行 / 指定行號 |
| **游標移動** | [[#Ctrl+u／Ctrl+d 視窗捲動\|Ctrl+u / Ctrl+d]] | 向上捲動半屏 / 向下捲動半屏 |
| **編輯動詞** | [[#d 刪除與剪下\|d]] | 刪除（剪下）目標文字物件或範圍（如 `dd`, `dw`, `diw`） |
| **編輯動詞** | [[#c 修改並進入插入模式\|c]] | 刪除目標文字並立即進入 Insert 模式（如 `ciw`, `ca"`） |
| **編輯動詞** | [[#y 複製\|y]] | 複製（Yank）目標文字物件或範圍（如 `yy`, `yi(`） |
| **編輯動詞** | [[#p／P 貼上\|p / P]] | 在游標後 / 游標前貼上暫存器內容 |
| **編輯動詞** | [[#u／Ctrl+r 復原與重做\|u / Ctrl+r]] | 復原上一步操作 / 重做被復原的操作 |
| **編輯動詞** | [[#. 重複上次修改\|.]] | 重複執行最後一次的修改動作（Vim 效率核心） |
| **文本物件** | [[#文本物件 (Text Objects) 核心文法\|i / a]] | 內部 (Inner) / 包含邊界 (Around) 文本物件語法 |
| **搜尋替換** | [[#／ 與 ? 搜尋導航\|/ / ? / * / #]] | 全文搜尋關鍵字 / 快速搜尋當前游標所在詞 |
| **搜尋替換** | [[#:%s 全域替換\|:%s/old/new/g]] | 全域搜尋與批次替換指令 |
| **VS Code 特性** | [[#gd／gh／gD 程式碼智慧跳轉\|gd / gh / gD]] | 跳轉至定義 / 顯示懸停資訊 / 跳轉至宣告 |
| **VS Code 特性** | [[#gb 多游標選取\|gb]] | 新增下一個相同單詞的多游標（VS Code 專屬殺手級整合） |
| **VS Code 特性** | [[#zc／zo／za 程式碼折疊\|zc / zo / za]] | 折疊 / 展開 / 切換當前程式碼區塊折疊狀態 |
| **擴充功能** | [[#vim-surround 符號包覆快速修改\|ys / cs / ds]] | 添加 / 修改 / 刪除周圍成對符號（引號、括號、HTML標籤） |

---

# 模式切換與狀態機制

## 核心模式總覽

Vim 在任何時刻都處於某個具體模式中

狀態列（VS Code 底部）會即時顯示當前所處的狀態：
- **NORMAL**：常態導航與命令執行模式
- **INSERT**：文字輸入模式
- **VISUAL**：文字選取模式
- **VISUAL LINE**：整行選取模式
- **VISUAL BLOCK**：矩形區塊選取模式
- **COMMAND-LINE**：底行命令模式（輸入 `:` 觸發）

---

## 進入 Insert 模式

##### i／I 進入插入模式

- **使用時機**：需要從目前游標所在位置或當前行開頭開始輸入文字時使用。
- **語法**：`i` 或 `I`
- **操作說明**：
  - `i`：在當前游標**前**進入 Insert 模式。
  - `I`：跳轉至當前行**第一個非空白字元前**並進入 Insert 模式。

```text
範例字串：    const total = 100;
游標在 total 的 't' 上
按下 i ➔ 游標停在 't' 前，直接輸入
按下 I ➔ 游標瞬間跳到 'const' 的 'c' 前，直接輸入
```

---

##### a／A 進入插入模式

- **使用時機**：需要從目前游標後方或行尾繼續追加文字時使用。
- **語法**：`a` 或 `A`
- **操作說明**：
  - `a` (Append)：在當前游標**後**進入 Insert 模式。
  - `A`：跳轉至當前**行尾最後一個字元後**並進入 Insert 模式。

```text
範例字串：    const total = 100;
游標在 total 的 'l' 上
按下 a ➔ 游標停在 'l' 後方空白處開始輸入
按下 A ➔ 游標瞬間跳到分號 ';' 後方開始輸入
```

---

##### o／O 新增行進入插入模式

- **使用時機**：需要在當前行的上方或下方立即插入新行開始編寫時使用，無需先移至行尾按 Enter。
- **語法**：`o` 或 `O`
- **操作說明**：
  - `o`：在當前行**下方**插入空白新行，並進入 Insert 模式。
  - `O`：在當前行**上方**插入空白新行，並進入 Insert 模式。

---

##### Esc／Ctrl+[ 返回一般模式

- **使用時機**：編輯完成後，立即退出 Insert 或 Visual 模式，返回 Normal 模式。
- **語法**：`Esc` 或 `Ctrl+[`
- **操作說明**：
  - 中斷當前輸入狀態，鎖定游標位置，回到 Normal 模式接收指令。

> **小提醒**：  
> 在標準鍵盤上 `Esc` 鍵距離較遠，推薦使用 `Ctrl+[` 或透過 `settings.json` 綁定 `jj` / `jk` 快速返回 Normal 模式。

---

## 可視選取模式 Visual Mode

##### v／V／Ctrl+v 可視模式

- **使用時機**：需要直觀選取一段連續字元、連續多行或矩形文字區塊進行批次操作時使用。
- **語法**：`v`、`V` 或 `Ctrl+v`
- **模式說明**：

| 快捷鍵 | 進入模式 | 選取範圍與特性 |
| :--- | :--- | :--- |
| `v` | **Visual (字元選取)** | 以字元為最小單位，游標移動到哪就選取到哪。 |
| `V` | **Visual Line (行選取)** | 以整行為單位，游標上下移動時自動包含整行所有字元。 |
| `Ctrl+v` | **Visual Block (區塊選取)** | 矩形多行垂直區塊選取，適合多行開頭批次插入或對齊修改。 |

> **區塊選取批次插入技巧**：  
> 1. 按 `Ctrl+v` 進入區塊模式，利用 `j` / `k` 向下向下選取多行同一欄位  
> 2. 按 `Shift+i` (大寫 `I`) 進入插入模式  
> 3. 輸入要批次插入的文字（此時只會在第一行顯示）  
> 4. 按下 `Esc`，選取的所有行將瞬間自動同步補上該文字！

---

# 游標移動導航 Motions

Vim 的移動指令不僅用於導航，更是所有編輯動詞的「作用範圍參數」

---

## 基礎方向與單詞跳轉

##### h／j／k／l 基礎方向導航

- **使用時機**：微調游標位置，雙手無需離開鍵盤主鍵區（Home Row）。
- **語法**：`h`、`j`、`k`、`l`
- **按鍵對照**：
  - `h`：向左 (Left)
  - `j`：向下 (Down)
  - `k`：向上 (Up)
  - `l`：向右 (Right)
- **前綴倍數**：支援數字前綴，例如 `5j` 代表向下移動 5 行，`10l` 代表向右移動 10 個字元。

---

##### w／b／e 單詞導航

- **使用時機**：以單詞為步進單位在程式碼中快速跨越。
- **語法**：`w` / `W`、`b` / `B`、`e` / `E`、`ge`
- **行為對照**：

| 鍵位 | 方向 | 跳轉目標位置 | 標點符號處理規則 |
| :--- | :--- | :--- | :--- |
| `w` | 向前 (Forward) | 下一個單詞的**開頭** | 將標點符號（如 `.`, `(`, `)`）視為獨立單詞 |
| `W` | 向前 (Forward) | 下一個**大單詞**的開頭 | 僅以**空白鍵**作為分隔，忽略標點符號 |
| `b` | 向後 (Backward) | 前一個單詞的**開頭** | 將標點符號視為獨立單詞 |
| `B` | 向後 (Backward) | 前一個**大單詞**的開頭 | 僅以空白鍵作為分隔 |
| `e` | 向前 (Forward) | 當前或下一個單詞的**結尾** | 將標點符號視為獨立單詞 |
| `E` | 向前 (Forward) | 當前或下一個大單詞的結尾 | 僅以空白鍵作為分隔 |
| `ge` | 向後 (Backward) | 前一個單詞的結尾 | 將標點符號視為獨立單詞 |

---

## 行內精準定位

##### 0／^／$ 行內邊界跳轉

- **使用時機**：在行內快速跳躍到最左端、有效程式碼開頭或最右端。
- **語法**：`0`、`^`、`$`
- **行為對照**：
  - `0`：瞬間移動到當前行的**絕對第 1 格（包含縮排空格）**。
  - `^`：瞬間移動到當前行的**第一個非空白字元（有效程式碼開頭）**。
  - `$`：瞬間移動到當前行的**行尾最後一個字元**。

---

##### f／F／t／T 行內字元精準跳轉

- **使用時機**：在當前行內快速跳轉到某個特定符號或字母上（行內狙擊槍）。
- **語法**：`f{char}`、`F{char}`、`t{char}`、`T{char}`、`;`、`,`
- **行為說明**：
  - `f{char}` (find)：向後尋找並將游標**停在**該字元上（例如 `f"` 直接跳到下一個引號）。
  - `F{char}`：向前（向左）尋找並將游標停在該字元上。
  - `t{char}` (till)：向後尋找並將游標停在該字元的**前一格**（常搭配 `dt"` 刪除到引號前）。
  - `T{char}`：向前尋找並將游標停在該字元的後一格。
  - `;`：重複上一次的 `f/F/t/T` 跳轉動作。
  - `,`：反方向重複上一次的 `f/F/t/T` 跳轉動作。

```text
範例行：    result = calculate_tax(user_id, rate);
游標在行首
輸入 f( ➔ 游標直接落在 '(' 上
輸入 dt, ➔ 瞬間刪除從 '(' 到逗號前所有內容，結果變為：result = calculate_tax(, rate);
```

---

## 跨行、全文與螢幕捲動

##### gg／G／行號跳轉 檔案全文跳轉

- **使用時機**：在檔案頭、尾或特定程式碼行之間長距離跳轉。
- **語法**：`gg`、`G`、`:[行號]` 或 `[行號]G`
- **行為說明**：
  - `gg`：跳轉至檔案第一行。
  - `G`：跳轉至檔案最後一行。
  - `:[行號]` + Enter 或 `[行號]G`：直接跳轉到指定行號（例如 `45G` 或 `:45` 跳到第 45 行）。
  - `%`：在成對的成對符號間跳轉（支援 `()`, `{}`, `[]`）。

---

##### Ctrl+u／Ctrl+d 視窗捲動

- **使用時機**：快速瀏覽程式碼上下文，無需頻繁按 `j` / `k`。
- **語法**：`Ctrl+u`、`Ctrl+d`、`Ctrl+b`、`Ctrl+f`、`zz`、`zt`、`zb`
- **指令清單**：

| 快捷鍵 | 功能說明 |
| :--- | :--- |
| `Ctrl+u` | (Up) 向上捲動半個螢幕高度。 |
| `Ctrl+d` | (Down) 向下捲動半個螢幕高度。 |
| `Ctrl+b` | (Back) 向上捲動一整個螢幕高度。 |
| `Ctrl+f` | (Forward) 向下捲動一整個螢幕高度。 |
| `zz` | 將當前游標所在行**置中於螢幕正中央**（保持視線舒適）。 |
| `zt` | (Top) 將當前游標所在行置頂於螢幕頂端。 |
| `zb` | (Bottom) 將當前游標所在行置底於螢幕底端。 |
| `H` | (High) 游標跳至目前螢幕可視區域的最頂行。 |
| `M` | (Middle) 游標跳至目前螢幕可視區域的中間行。 |
| `L` | (Low) 游標跳至目前螢幕可視區域的最底行。 |

---

# 核心編輯動詞與組合技 Operators

Vim 的真正威力來自於動詞與目標範圍的任意組合

---

## 核心編輯操作

##### d 刪除與剪下

- **使用時機**：移除指定範圍的程式碼（在 Vim 中刪除等同於剪下至暫存器）。
- **語法**：`d[motion]` 或 `dd`
- **常見組合**：
  - `dd`：刪除當前整行。
  - `3dd`：刪除當前行及下方共 3 行。
  - `dw`：刪除從游標到下一個單詞開頭的字元。
  - `d$` 或 `D`：刪除從游標到當前行尾的所有字元。
  - `d0`：刪除從游標到當前行首的所有字元。
  - `dgg`：刪除從當前行到檔案第一行的所有內容。
  - `dG`：刪除從當前行到檔案最後一行的所有內容。

---

##### c 修改並進入插入模式

- **使用時機**：刪除舊內容並立即準備寫入新內容（Change 動作 = 刪除 + 進入 Insert 模式）。
- **語法**：`c[motion]` 或 `cc`
- **常見組合**：
  - `cc`：清空當前整行內容並進入 Insert 模式（保持既有縮排）。
  - `cw`：刪除當前游標到該單詞結尾並進入 Insert 模式。
  - `c$` 或 `C`：刪除從游標到行尾並進入 Insert 模式。
  - `ciw`：清空當前單詞並進入 Insert 模式。

---

##### y 複製

- **使用時機**：複製目標程式碼到暫存器中（Yank）。
- **語法**：`y[motion]` 或 `yy`
- **常見組合**：
  - `yy` 或 `Y`：複製當前整行。
  - `3yy`：複製當前行開始的連續 3 行。
  - `yw`：複製當前游標到下一個單詞開頭。
  - `y$`：複製從游標到行尾的所有文字。

---

##### p／P 貼上

- **使用時機**：將最近複製（`y`）或刪除（`d`）的文字貼到編輯器中。
- **語法**：`p` 或 `P`
- **行為對照**：
  - 若剪貼內容為**字元/單詞**：
    - `p`：貼在當前游標**後**。
    - `P`：貼在當前游標**前**。
  - 若剪貼內容為**整行**：
    - `p`：貼在當前行**下方**另起一行。
    - `P`：貼在當前行**上方**另起一行。

---

##### u／Ctrl+r 復原與重做

- **使用時機**：修復誤操作或重做被復原的變更。
- **語法**：`u` 與 `Ctrl+r`
- **行為說明**：
  - `u` (Undo)：復原上一次的完整操作（一次 Insert 模式的輸入視為單一動作）。
  - `Ctrl+r` (Redo)：重做（取消復原）被 `u` 撤銷的變更。

---

##### . 重複上次修改

- **使用時機**：需要對多處程式碼進行完全相同的修改時，大幅節省重複敲擊鍵盤的時間。
- **語法**：`.`
- **運作機制**：
  - `.` 會自動重播上一次對文字內容產生實質變更的指令（包括動詞、文本物件或 Insert 模式內的輸入）。

```text
情境：將多個變數名稱後方補上預設值 '= None;'
1. 在第 1 行按 A 輸入 '= None;' 並按 Esc
2. 移到第 2 行目標位置，直接按 . ➔ 自動補上 '= None;'
3. 移到第 3 行目標位置，直接按 . ➔ 再次自動補上！
```

---

## 常用單字元快速編輯

| 指令 | 動作名稱 | 詳細說明 |
| :--- | :--- | :--- |
| `x` | 刪除單一字元 | 刪除當前游標所在的字元（等同 Delete 鍵）。 |
| `X` | 向前刪除字元 | 刪除當前游標左側的字元（等同 Backspace 鍵）。 |
| `s` | 替換單一字元 | 刪除當前字元並立即進入 Insert 模式。 |
| `S` | 替換整行 | 刪除當前整行並進入 Insert 模式（等同 `cc`）。 |
| `r{char}` | 單字元就地替換 | 將游標所在字元替換為 `{char}`，完成後**保持在 Normal 模式**。 |
| `R` | 取代模式 (Replace) | 進入 Replace 模式，後續輸入將逐字覆蓋現有文字。 |
| `~` | 大小寫翻轉 | 將游標所在字元切換大/小寫，並將游標向右移一格。 |
| `J` | 行合併 (Join) | 將當前行與下一行合併為一行，自動移除多餘換行與縮排。 |
| `>>` / `<<` | 縮排調整 | 當前行向右縮排 / 向左減少縮排一次。 |

---

# 文本物件 (Text Objects) 核心文法

文本物件是 Vim 最具殺傷力的結構化編輯機制

它允許你不必將游標移動到開頭或結尾，**只要游標位於該結構內部任何一處，就能直接對整個結構進行操作**

文法結構公式：

$$\text{動詞 (Operator)} + \text{範圍修飾詞 (i / a)} + \text{物件類型 (Object)}$$

- `i` (Inner)：**內部**，不包含外層的圍繞符號或空格。
- `a` (Around / A)：**包含邊界**，連同外層的圍繞符號或外側空格一併處理。

---

## 常用文本物件對照表

| 物件識別字 | 代表物件類型 | `i` 範例與效果 | `a` 範例與效果 |
| :--- | :--- | :--- | :--- |
| `w` | 單詞 (Word) | `diw`：僅刪除單詞文字本身 | `daw`：刪除單詞文字並**連同相鄰空格**一併刪除 |
| `W` | 大單詞 (Big Word) | `ciW`：修改整個連續無空格字串 | `caW`：修改大單詞並包含相鄰空格 |
| `"` | 雙引號字串 | `ci"`：清空雙引號內部的字串 | `ca"`：連同兩側的雙引號 `""` 整組刪除 |
| `'` | 單引號字串 | `ci'`：清空單引號內部的字串 | `ca'`：連同兩側的單引號 `''` 整組刪除 |
| `` ` `` | 反引號範本字串 | `ci\``：清空反引號內部字串 | `ca\``：連同反引號整組刪除 |
| `(` 或 `)` 或 `b` | 小括號區塊 | `di(`：刪除函式參數括號內所有內容 | `da(`：連同外層小括號 `()` 一起刪除 |
| `{` 或 `}` 或 `B` | 大括號區塊 | `ci{`：清空整個程式碼區塊內部 | `ca{`：連同外層大括號 `{}` 一起刪除 |
| `[` 或 `]` | 中括號陣列 | `vi[`：選取陣列內部所有元素 | `va[`：選取包含中括號的整個陣列 |
| `<` 或 `>` | 角括號 / 泛型 | `di<`：刪除泛型內型別名稱 | `da<`：連同角括號 `<>` 一起刪除 |
| `t` | HTML/XML Tag 標籤 | `cit`：清空 HTML 標籤內部的子元素 | `cat`：連同 `<tag>...</tag>` 整個節點刪除 |

---

## 實戰對比範例

```html
<!-- 範例：游標停在 'Title' 的 't' 上 -->
<div class="header">
    <h1>Page Title</h1>
</div>
```

- 按下 `ciw` ➔ 變為 `<h1>Page </h1>`，游標停在空格後等待輸入新單詞。
- 按下 `cit` ➔ 變為 `<h1></h1>`，直接清空整個標籤內部。
- 按下 `cat` ➔ 整個 `<h1>Page Title</h1>` 被連根拔起刪除。

---

# 搜尋、替換與全域操作

---

## 快速搜尋導航

##### ／ 與 ? 搜尋導航

- **使用時機**：在檔案中快速搜尋特定關鍵字或符號。
- **語法**：`/{pattern}`、`?{pattern}`、`*`、`#`
- **操作說明**：
  - `/{pattern}` + Enter：由當前位置**向下**搜尋符合的字串。
  - `?{pattern}` + Enter：由當前位置**向上**搜尋符合的字串。
  - `n`：跳至下一個符合項（Next）。
  - `N`：跳至上一個符合項（Previous）。
  - `*`：直接將**當前游標所在單詞**作為關鍵字向下搜尋。
  - `#`：直接將**當前游標所在單詞**作為關鍵字向上搜尋。

> **小提醒：清除搜尋高亮**：  
> 搜尋完成後畫面上常會殘留黃色高亮色塊，輸入 `:noh` 或 `:nohlsearch` 並按 Enter 即可瞬間清除高亮。

---

## Ex 命令列全域替換

##### :%s 全域替換

- **使用時機**：對當前檔案或指定範圍進行規則化批次搜尋替換。
- **語法**：`:[範圍]s/{搜尋目標}/{替換內容}/[旗標]`
- **參數說明**：

| 範圍 / 旗標 | 代表意義 | 說明 |
| :--- | :--- | :--- |
| `%` | 全文範圍 | 代表第 1 行到最後一行（等同 `1,$`）。 |
| `s` | 替換 (Substitute) | 觸發文字替換指令。 |
| `g` | 全域旗標 (Global) | 一行中若有多個相符項，全部替換（若不加 `g` 則只替換每行第 1 個）。 |
| `i` | 忽略大小寫 | 不區分大小寫進行比對。 |
| `c` | 逐一確認 (Confirm) | 替換每處前先彈出提示詢問 `(y/n/a/q)`。 |

```vim
...
" 1. 全文替換所有 old_func 為 new_func
:%s/old_func/new_func/g

" 2. 全文替換並在每次替換前要求人工確認
:%s/temp_var/final_var/gc

" 3. 僅在第 10 行到第 25 行之間進行替換
:10,25s/var/let/g
...
```

---

# 暫存器 (Registers) 與巨集 (Macros)

---

## 暫存器機制

在 Vim 中，剪下與複製的內容存放在不同的「暫存器 (Registers)」中，而非單一剪貼簿

使用 `"{register}` 前綴指定要讀寫的暫存器

##### 常用暫存器分類

| 暫存器代號 | 暫存器名稱 | 特性與使用場景 |
| :--- | :--- | :--- |
| `""` | **未命名暫存器 (Unnamed)** | 預設暫存器。所有 `d`, `c`, `s`, `x`, `y` 內容都會自動寫入這裡。 |
| `"0` | **專用複製暫存器 (Yank)** | 永遠只保存最近一次 **`y` (複製)** 的內容，不會被後續的 `d` (刪除) 覆蓋！ |
| `"+` 或 `"*` | **系統剪貼簿 (Clipboard)** | 與作業系統（macOS / Windows）外部剪貼簿同步共用。 |
| `"_` | **黑洞暫存器 (Black Hole)** | 相當於 `/dev/null`。丟進去的內容徹底消失，完全不覆蓋任何暫存器。 |
| `"a` ~ `"z` | **具名自訂暫存器** | 26 個專屬儲存槽，供長期暫存特定文字片斷。 |

> **開發避坑天條：解決「複製文字後，刪除舊程式碼導致複製內容被覆蓋」**：  
> 1. 先 `yy` 複製了目標程式碼  
> 2. 游標移到舊程式碼處使用 `dd` 刪除  
> 3. 此時如果直接按 `p`，貼出的會是剛才刪除的垃圾！  
> **解法 A**：改用 `"0p` 貼上（強制讀取複製專用暫存器）。  
> **解法 B**：使用黑洞暫存器刪除 `"_dd` 或 `"_ciw`，不污染預設剪貼簿。

---

## 巨集 (Macros) 批次自動化錄製

巨集可以將一連串複雜的鍵位操作錄製下來，並對任意多行瞬間重播

##### 巨集錄製與重播指令

- **使用時機**：處理結構相似但不完全一致的重複文字重構（如資料轉換、JSON 對齊）。
- **語法**：`q{register}` 開始錄製，`q` 停止錄製，`@{register}` 播放。
- **步驟流程**：
  1. **開始錄製**：在 Normal 模式下按下 `q` 加上一個字母（如 `qa` 代表錄製到暫存器 a）。
  2. **執行編輯動作**：進行一次完整的單行重構操作（注意：操作結尾應將游標移動到下一行的起始定位點，確保可重播性）。
  3. **結束錄製**：再次按下 `q`。
  4. **單次重播**：輸入 `@a` 執行一次巨集。
  5. **批次重播**：輸入 `10@a` 瞬間重播 10 次，或在 Visual 模式選取多行後輸入 `:normal @a`。

---

# VS Code 專屬整合與進階特性

VS Code Vim 插件將 Vim 模態邏輯與 VS Code 的現代 IDE 能力進行了深度融合

---

## VS Code 程式碼導航與 IntelliSense 整合

##### gd／gh／gD 程式碼智慧跳轉

- **使用時機**：在專案中快速追蹤符號原始宣告與檢視型別資訊。
- **語法**：`gd`、`gD`、`gh`、`gf`
- **行為對照**：
  - `gd` (Go to Definition)：直接跳轉到游標所在函式/變數的**定義處**（等同 F12）。
  - `gD` (Go to Declaration)：跳轉至符號的**宣告處**。
  - `gh` (Hover)：彈出 VS Code 的 **Hover 提示浮窗**（檢視函式簽名、文檔或型別，等同滑鼠懸停）。
  - `gf` (Go to File)：若游標在檔案路徑字串上，直接在編輯器開啟該檔案。
  - `Ctrl+o`：跳轉回上一個游標歷史位置（Jump List Back）。
  - `Ctrl+i`：跳轉前進到下一個游標歷史位置（Jump List Forward）。

---

##### gb 多游標選取

- **使用時機**：需要同時修改檔案中多處相同的變數或單詞時使用。
- **語法**：`gb`
- **操作說明**：
  - 在 Normal 模式下，游標停在某個單詞上按下 `gb`。
  - 插件會自動將該單詞選取為可視狀態，每多按一次 `gb`，就會在檔案中**下一個相同單詞處新增一個多游標 (Multi-cursor)**。
  - 接著可直接按 `c` 進行批次同步修改。

---

##### zc／zo／za 程式碼折疊

- **使用時機**：大檔案中收合不關心的函式區塊，提升閱讀專注度。
- **語法**：`zc`、`zo`、`za`、`zM`、`zR`
- **指令清單**：

| 快捷鍵 | 功能 |
| :--- | :--- |
| `zc` | (Close) 折疊當前程式碼區塊。 |
| `zo` | (Open) 展開當前程式碼區塊。 |
| `za` | (Toggle) 切換當前區塊的折疊/展開狀態。 |
| `zM` | 折疊當前檔案內**所有**可折疊區塊。 |
| `zR` | 展開當前檔案內**所有**折疊區塊。 |

---

## 內建擴充：vim-surround 成對符號管理

VS Code Vim 內建模擬了著名的 `vim-surround` 外掛（預設啟用）

專門用來極速處理包圍在文字外側的括號、引號與 HTML 標籤

##### vim-surround 符號包覆快速修改

- **核心動作**：`ds` (Delete Surround)、`cs` (Change Surround)、`ys` (You Surround)

| 指令格式 | 動作名稱 | 範例原始文字 | 執行指令 | 變更後結果 |
| :--- | :--- | :--- | :--- | :--- |
| `ds{char}` | 刪除外圍符號 | `"Hello World"` | `ds"` | `Hello World` |
| `cs{old}{new}` | 替換外圍符號 | `'Hello World'` | `cs'"` | `"Hello World"` |
| `cs{old}{new}` | 替換為括號 | `"Hello"` | `cs"(` | `( Hello )` (有空格) |
| `cs{old}{new}` | 替換為緊貼括號 | `"Hello"` | `cs")` | `(Hello)` (無空格) |
| `ys{motion}{char}` | 新增外圍符號 | `total_count` (游標在單詞上) | `ysiw"` | `"total_count"` |
| `ys{motion}{tag}` | 包覆 HTML 標籤 | `Hello` | `ysiw<em>` | `<em>Hello</em>` |

---

## 內建擴充：vim-commentary 快速註釋

- **語法**：`gcc` 或 `gc[motion]`
- **操作說明**：
  - `gcc`：切換當前行的註釋狀態（支援任何程式語言原生語法）。
  - `gc3j`：註釋當前行及下方 3 行。
  - `gcap`：註釋當前整個段落 (Paragraph)。
  - 在 Visual 模式下選取範圍後按 `gc`：批次註釋選取區塊。

---

# VS Code settings.json 推薦生產力配置

在 VS Code 的 `settings.json` 中配置 Vim 參數，能大幅消除日常打字痛點

```json
{
    // 啟用 VS Code 系統剪貼簿無縫共用 (複製貼上直接與外部系統同步)
    "vim.useSystemClipboard": true,

    // 搜尋時啟用即時高亮
    "vim.hlsearch": true,

    // 啟用內建 vim-surround 外掛支援
    "vim.surround": true,

    // 啟用 easymotion 快速導航外掛支援
    "vim.easymotion": true,

    // 自訂 Leader 鍵 (推薦設為空白鍵 Space)
    "vim.leader": "<space>",

    // 插入模式下鍵位映射：連續輸入 jk 或 jj 瞬間退出 Insert 模式回到 Normal 模式
    "vim.insertModeKeyBindings": [
        {
            "before": ["j", "k"],
            "after": ["<Esc>"]
        },
        {
            "before": ["j", "j"],
            "after": ["<Esc>"]
        }
    ],

    // 一般模式下的快捷鍵映射 (利用 Leader 鍵調用 VS Code 原生命令)
    "vim.normalModeKeyBindingsNonRecursive": [
        {
            // 按 Space + w 快速儲存檔案
            "before": ["<leader>", "w"],
            "commands": ["workbench.action.files.save"]
        },
        {
            // 按 Space + f 快速格式化當前檔案
            "before": ["<leader>", "f"],
            "commands": ["editor.action.formatDocument"]
        },
        {
            // 按 Space + h 清除搜尋高亮
            "before": ["<leader>", "h"],
            "commands": [":nohl"]
        }
    ]
}
```

---

# 實戰避坑與開發天條

> **天條一：嚴禁用連按方向鍵或 Backspace 進行大範圍移動與刪除**  
> 在 Normal 模式下，善用 `w`, `b`, `f`, `t`, `ciw`, `di(` 等結構化指令  
> 連續敲擊 `h/j/k/l` 或長按 Backspace 徹底失去了模態編輯的效率優勢

> **天條二：區分小括號 `(` 與 `)` 在 surround 插件中的空白行為**  
> 使用 `cs"(` 會在括號內側自動補上**空格**（例如 `( text )`）  
> 使用 `cs")` 則會**緊貼文字**不留空格（例如 `(text)`）

> **天條三：善用 `.` (句點) 建立可重複的小步編輯慣性**  
> 養成「精準動詞 + 文本物件（如 `ciw` 或 `caw`）」的習慣，後續只要移動到目標單詞按 `.` 即可瞬間完成修改  
> 若直接進入 Insert 模式手動刪除再打字，`.` 指令將無法正確重播完整的意圖
