這是給予電腦核心的最直接指令，也就是在終端機 (Terminal) 中敲擊的文字命令。

> **🍿 比喻單元**：
> 想像 Shell 就像餐廳裡的**服務生**：你（使用者）輸入指令點餐，服務生把菜單翻譯並傳給廚房（系統核心），廚房做完菜後，服務生再把結果端上桌呈現給你！

---

## 核心概念

### Shell 與 Zsh
Shell 是使用者與系統核心間的「翻譯官」。電腦只懂二進位，而我們寫文字，Shell 負責將兩者轉換。
- **bash**：歷史最悠久、開源社群最大的標準 Shell，絕大多數伺服器腳本都用它。
- **zsh**：macOS 預設的現代 Shell，具備強大自動補全、語法高亮與外掛生態。

每次開啟終端機時，zsh 會自動載入 `~/.zshrc` 作為設定檔（修改後需執行 `source ~/.zshrc` 才能生效）。

### 環境變數 PATH
PATH 是作業系統搜尋可執行檔的**導航地圖與地址簿**。
當你輸入 `python` 時，系統會去 PATH 裡登記的資料夾依序尋找同名的實體程式，找不到就會拋出著名的 `zsh: command not found`。

### 標準 I/O 與管道重導向
資料流（Data Stream）就像連續不斷的水流，分為三大類：
1. **stdin (標準輸入)**：代號 `0`，預設為鍵盤。
2. **stdout (標準輸出)**：代號 `1`，預設為螢幕。
3. **stderr (標準錯誤)**：代號 `2`，預設為螢幕錯誤。

你可以透過重導向把水流轉向檔案：
- `>`：覆蓋寫入。
- `>>`：附加寫入。
- `|` (Pipe 管道)：把前一個指令流出的水 (stdout) 直接接到下一個指令的進水口 (stdin)。

### 檔案權限 (755)
Unix 權限像展覽館的門禁卡，對三種身份賦予三種權限 (`r(4) + w(2) + x(1)`)：
- **u (User)**：個人擁有者。
- **g (Group)**：所屬群組。
- **o (Others)**：其他所有人。
> **💡 小提醒**：755 代表：User(`4+2+1=7` 可讀寫執行)、Group(`4+1=5` 可讀可執行)、Others(`5` 可讀可執行)。

### Metadata 與 Timestamp (時間截記)
檔案除了內容外，還有用來描述它的 Metadata（如拍攝時間）。
每個檔案必有三種時間：
- **mtime (修改時間)**：檔案「內容」最後修改時間。
- **atime (存取時間)**：檔案最後被「讀取」時間。
- **ctime (狀態改變時間)**：檔案「元數據」最後改變時間（基於安全性，無法隨意篡改為指定時間）。

### 進程與 PID
進程 (Process) 是執行中的程式。
- **PID (Process ID)**：系統給進程的「身分證字號/動態桌號」。當程式卡死時，就是透過它來精準「翻桌」結束程式。

---

## 路徑與目錄操作

##### pwd 顯示當前路徑

- **使用時機**：當你在終端機裡迷路，想確認自己目前位在哪個資料夾的絕對路徑時。
- **語法**：`pwd`
- **參數說明**：無。
- **回傳值/輸出效果**：印出目前所在的絕對路徑字串。

```shell
...
# 1. 印出當前路徑 (Print Working Directory)
pwd
# 輸出: /Users/matthew/Documents/Obsidian Vault
...
```

---

##### ls 列出檔案清單

- **使用時機**：想查看當下目錄裡有哪些檔案和資料夾，或者是想看某個檔案的詳細權限與修改時間時。
- **語法**：`ls [-l] [-a] [-h] [路徑]`
- **參數說明**：

| 參數 | 說明 |
| :--- | :--- |
| `-l` | (long) 長格式詳細資訊，包含權限、擁有者、時間截記與大小。 |
| `-a` | (all) 顯示所有檔案，包含開頭為 `.` 的隱藏檔。 |
| `-h` | (human-readable) 將檔案大小轉為好讀的 KB/MB/GB（需搭配 `-l`）。 |

- **回傳值/輸出效果**：印出目錄下的檔案名稱或詳細清單。

```shell
...
# 1. 組合技：顯示包含隱藏檔的詳細資訊與人性化大小
ls -lah
...
```

> **💡 小提醒**：`ls -lah` 輸出的 `drwxr-xr-x` 第一個字元若為 `d` 代表它是 Directory (目錄)，若為 `-` 代表是一般檔案。

---

##### cd 切換目錄

- **使用時機**：想進入另一個資料夾，或回到上一層資料夾時。
- **語法**：`cd [路徑]`
- **參數說明**：

| 參數/路徑 | 說明 |
| :--- | :--- |
| `..` | 代表父目錄（回上一層）。 |
| `~` | 代表當前使用者的 Home 家目錄 (`/Users/使用者`)。 |
| `-` | 代表相鄰的「前一個停留的目錄」（像電視遙控器的返回鍵）。 |

- **回傳值/輸出效果**：改變當前的終端機工作目錄。

```shell
...
# 1. 切換到指定目錄
cd Document/test

# 2. 快速退回上一層
cd ..
...
```

---

##### clear 清除終端輸出

- **使用時機**：當終端機畫面充滿了一大堆之前的輸出，看起來很亂想還原乾淨版面時。
- **語法**：`clear` (或使用快捷鍵 `Cmd + K` / `Ctrl + L`)
- **參數說明**：無。
- **回傳值/輸出效果**：清除所有過往輸出，將提示字元推至最上方。

---

##### tree 印出樹狀目錄結構

- **使用時機**：接手新專案時，想以視覺化樹狀圖快速了解整個專案資料夾結構時。
- **語法**：`tree [-L 階層數]`
- **參數說明**：

| 參數 | 說明 |
| :--- | :--- |
| `-L` | (Level) 限制顯示階層深度，避免檔案太多導致洗頻。 |

- **回傳值/輸出效果**：印出結構圖。

```shell
...
# 1. 限制只展開前 2 層
tree -L 2
...
```

---

##### find 搜尋檔案與目錄

- **使用時機**：當你忘記某個檔案放在哪裡，或者想批次找出所有 `.py` 檔案時。
- **語法**：`find [搜尋路徑] [-name 名稱] [-type 類型]`
- **參數說明**：

| 參數 | 說明 |
| :--- | :--- |
| `-name` | 依據檔案名稱搜尋（支援 `*` 等萬用字元，需加引號）。 |
| `-type` | 依據類型搜尋：`f` 代表檔案 (file)，`d` 代表目錄 (directory)。 |

- **回傳值/輸出效果**：印出所有符合條件的檔案絕對或相對路徑。

```shell
...
# 1. 在當前目錄 (.) 下尋找所有副檔名為 py 的檔案
find . -name "*.py"

# 2. 限定只找「檔案(f)」且名稱以 test 開頭
find . -type f -name "test*"
...
```

---

## 檔案管理

##### mkdir 建立資料夾

- **使用時機**：當你需要新增一個或多個全新的目錄時。
- **語法**：`mkdir [-p] 資料夾名稱`
- **參數說明**：

| 參數 | 說明 |
| :--- | :--- |
| `-p` | (parents) 遞迴建立，若父目錄不存在會一併自動建立，且若目錄已存在則不會報錯。 |

- **回傳值/輸出效果**：成功建立目錄，無文字回傳。

```shell
...
# 1. 遞迴建立多層目錄
mkdir -p project/src/components
...
```

---

##### touch 建立檔案與更新時間

- **使用時機**：最常用來快速建立一個空白檔案，或更新現有檔案的時間截記 (mtime/atime)。
- **語法**：`touch [-c] [-m] 檔案名`
- **參數說明**：

| 參數 | 說明 |
| :--- | :--- |
| `-c` | (no-create) 若檔案不存在則拒絕自動建立空白檔。 |
| `-m` | (modification) 僅更新修改時間。 |

- **回傳值/輸出效果**：無文字回傳。

```shell
...
# 1. 建立一個全新的空白腳本
touch script.py
...
```

---

##### cp 複製檔案或資料夾

- **使用時機**：想備份一份檔案，或是將整個專案資料夾拷貝一份到別處時。
- **語法**：`cp [-r] 來源 目的地`
- **參數說明**：

| 參數 | 說明 |
| :--- | :--- |
| `-r` | (recursive) 遞迴複製，用來複製「整個資料夾」及其內部的所有子檔案。 |

- **回傳值/輸出效果**：檔案或資料夾被成功複製。

```shell
...
# 1. 複製單一檔案
cp .env.example .env

# 2. 複製整個資料夾
cp -r src/ src_backup/
...
```

---

##### mv 移動或重新命名

- **使用時機**：想把檔案搬移到別的目錄，或者是想對現有檔案/資料夾重新命名時（在 Unix 系統中，重新命名本質上就是原地移動）。
- **語法**：`mv 來源 目的地`
- **參數說明**：無特殊常用參數。
- **回傳值/輸出效果**：檔案被成功移動或改名。

```shell
...
# 1. 重新命名檔案
mv old_name.txt new_name.txt

# 2. 將檔案移動到另一個資料夾
mv config.json ./settings/
...
```

---

##### rm 刪除檔案或資料夾

- **使用時機**：想徹底刪除某個檔案或清理整個無用的目錄時。
- **語法**：`rm [-r] [-f] 目標`
- **參數說明**：

| 參數 | 說明 |
| :--- | :--- |
| `-r` | (recursive) 遞迴刪除整個目錄結構（刪資料夾必備）。 |
| `-f` | (force) 強制刪除，不彈出任何詢問提示。 |

- **回傳值/輸出效果**：檔案永久刪除。

> **💡 小提醒**：`rm -rf` 刪除的檔案**不會經過垃圾桶**，操作不可逆，使用前請再三確認路徑！

```shell
...
# 1. 強制刪除整個資料夾與其內部所有內容 (極危險)
rm -rf node_modules/
...
```

---

##### cat 檢視或拼接檔案內容

- **使用時機**：檔案很小，想直接在終端機印出所有內容來檢視，或想合併多個檔案時。
- **語法**：`cat 檔案1 [檔案2...]`
- **參數說明**：無特殊常用參數。
- **回傳值/輸出效果**：印出檔案的所有文字內容。

```shell
...
# 1. 檢視單一小檔案
cat .gitignore

# 2. 將兩個檔案拼接並透過 > 覆蓋寫入新檔
cat part1.txt part2.txt > full.txt
...
```

---

##### less 分頁檢視長檔案

- **使用時機**：當檔案內容非常長（如伺服器 Log 檔），直接 `cat` 會瞬間刷滿螢幕，需要分頁慢慢滾動瀏覽時。
- **語法**：`less 檔案名`
- **參數說明**：無。
- **回傳值/輸出效果**：進入分頁閱讀模式。

> **💡 小提醒**：在閱讀模式中，按 `Space` 換頁，按 `b` 回上一頁，輸入 `/關鍵字` 可搜尋，按 `q` 離開。

---

##### grep 搜尋檔案內的文字

- **使用時機**：當你想在一個巨大的 Log 檔案中尋找 "Error" 關鍵字，或是想透過管道符 (`|`) 從 `ps aux` 的超長名單中只過濾出 Python 的進程時。它是終端機裡最強大的「文字過濾網」。
- **語法**：`grep [-i] [-v] [-r] 關鍵字 [檔案名]`
- **參數說明**：

| 參數 | 說明 |
| :--- | :--- |
| `-i` | (ignore case) 忽略大小寫。 |
| `-v` | (invert match) 反向過濾，只留下「沒有」包含關鍵字的行。 |
| `-r` | (recursive) 遞迴搜尋，會連同子資料夾裡的所有檔案一起找。 |

- **回傳值/輸出效果**：印出包含該關鍵字的完整行文字，並通常會將關鍵字上色。

```shell
...
# 1. 在 server.log 檔案中尋找 "error" (不分大小寫)
grep -i "error" server.log

# 2. 組合技：過濾進程名單，只找含有 python 的進程
ps aux | grep python
...
```

---

##### chmod 變更存取權限

- **使用時機**：當你寫了一支腳本（如 `.sh`）卻沒有執行權限 (`Permission denied`)，需要賦予它執行權限時。
- **語法**：`chmod 權限設定 檔案名`
- **參數說明**：權限設定可為數字（如 755）或符號（如 u+x）。
- **回傳值/輸出效果**：權限被成功修改。

```shell
...
# 1. 透過數字設定權限 (Owner全開，Group/Others唯讀執行)
chmod 755 script.sh

# 2. 透過符號為 User 加上執行權限 (add execute)
chmod u+x script.sh
...
```

---

##### chown 變更擁有者

- **使用時機**：當某個檔案的所有權屬於 `root`，導致你無法修改，需要將擁有者變更為你自己時。
- **語法**：`chown 擁有者[:群組] 檔案名`
- **參數說明**：無特殊常用參數。
- **回傳值/輸出效果**：擁有者權限被修改。

```shell
...
# 1. 變更擁有者與所屬群組
chown matthew:staff file.txt
...
```

---

##### du 計算硬碟佔用量

- **使用時機**：想知道某個專案資料夾到底佔用了多少硬碟空間時。
- **語法**：`du [-s] [-h] 目標`
- **參數說明**：

| 參數 | 說明 |
| :--- | :--- |
| `-s` | (summary) 僅顯示總計大小，不要列出內部每個子資料夾。 |
| `-h` | (human-readable) 以 KB/MB/GB 顯示。 |

- **回傳值/輸出效果**：印出計算出的容量大小。

```shell
...
# 1. 快速查看當前資料夾的總容量
du -sh .
...
```

---

##### df 檢視磁碟剩餘空間

- **使用時機**：想知道整台電腦或伺服器的磁碟空間還剩下多少時。
- **語法**：`df [-h]`
- **參數說明**：

| 參數 | 說明 |
| :--- | :--- |
| `-h` | (human-readable) 以 KB/MB/GB 顯示。 |

- **回傳值/輸出效果**：印出全機各大分區的剩餘容量。

```shell
...
df -h
...
```

---

## 系統與進程管理

##### open 開啟檔案或目錄

- **使用時機**：當你在終端機中，想用 macOS 預設的圖形化應用程式（如 Finder、預覽程式或瀏覽器）打開某個檔案或網址時。
- **語法**：`open [-a 應用程式] 目標`
- **參數說明**：

| 參數 | 說明 |
| :--- | :--- |
| `-a` | (application) 指定要用哪一個應用程式開啟。 |

- **回傳值/輸出效果**：呼叫對應的圖形化介面應用程式。

```shell
...
# 1. 用 Finder 打開目前所在的目錄 (.)
open .

# 2. 用預設瀏覽器打開網址
open https://www.google.com

# 3. 指定用 VS Code 打開檔案
open -a "Visual Studio Code" script.py
...
```

---

##### pbcopy 與 pbpaste() 讀寫剪貼簿

- **使用時機**：想把終端機印出來的超長 Token 或 Log 複製下來，或者想把複製的文字存成檔案時（macOS 專屬）。
- **語法**：`pbcopy` / `pbpaste`
- **參數說明**：無。
- **回傳值/輸出效果**：將資料流寫入系統剪貼簿或從剪貼簿讀出。

```shell
...
# 1. 將檔案內容複製到系統剪貼簿
cat id_rsa.pub | pbcopy

# 2. 將剪貼簿的文字存成檔案
pbpaste > log.txt
...
```

---

##### ps 檢視進程狀態

- **使用時機**：想查詢系統目前有哪些程式在背景運作，或者想找出某個卡死的程式的 PID 時。
- **語法**：`ps [參數]`
- **參數說明**：

| 參數 | 說明 |
| :--- | :--- |
| `a` | 顯示所有使用者的進程。 |
| `u` | 顯示詳細的使用者與記憶體佔用資訊。 |
| `x` | 包含沒有連線終端機的背景系統進程。 |

- **回傳值/輸出效果**：印出進程的詳細快照清單。

```shell
...
# 1. 顯示系統中所有運作中的詳細進程清單
ps aux
...
```

---

##### top 即時動態監控

- **使用時機**：當電腦風扇狂轉，你想知道是哪一個程式在吃 CPU 或記憶體時。
- **語法**：`top` 或 `htop`
- **參數說明**：無。
- **回傳值/輸出效果**：開啟一個即時刷新的監控介面（按 `q` 退出）。

> **💡 小提醒**：`htop` 是一個需要額外安裝的高級版 `top`，它擁有彩色的互動介面，非常推薦使用 `brew install htop` 來安裝它。

---

##### kill 終止指定進程

- **使用時機**：當某個程式卡死（無響應）或佔用資源，你需要手動將它關閉時。
- **語法**：`kill [-9] PID數字`
- **參數說明**：

| 參數 | 說明 |
| :--- | :--- |
| `-9` | (SIGKILL) 九號殺手權利，不給程式存檔準備的機會，強制立即抹殺。 |

- **回傳值/輸出效果**：發送訊號中斷程式。

```shell
...
# 1. 正常發送終止訊號
kill 1234

# 2. 強制抹殺無響應的進程
kill -9 1234
...
```

---

##### lsof 列出被開啟的檔案與通訊埠

- **使用時機**：在 Unix 系統中「一切皆檔案（包含網路連線）」。最常用在啟動伺服器卻跳錯 "Port is already in use" 時，用來查出佔用 Port 的真兇；或者是你想刪除某個檔案，系統卻提示「檔案正被使用中」時，用來找出是哪個進程卡住了它。
- **語法**：`lsof [-i] [-p] [檔案名稱]`
- **參數說明**：

| 參數     | 說明                                           |
| :----- | :------------------------------------------- |
| `-i`   | (internet) 篩選網路連線，通常接 `:Port號` 用來尋找佔用通訊埠的程式。 |
| `-p`   | (process) 接上 `PID數字`，可以列出該特定進程目前開啟了哪些檔案。     |
| `[檔名]` | 直接接上檔案或資料夾名稱，會列出目前正在使用該檔案的所有進程。              |

- **回傳值/輸出效果**：印出佔用該資源的程式清單（包含 COMMAND 與 PID）。

```shell
...
# 1. 網頁開發必備：查詢是哪個程式佔用了 8080 port
lsof -i :8080

# 2. 排查佔用：查詢是誰正在使用 database.db 這個檔案
lsof database.db

# 3. 監控進程：看看 PID 為 1234 的程式開啟了哪些檔案
lsof -p 1234
...
```

---

## 開發工具與環境

##### python3 直譯與執行 Python

- **使用時機**：執行 Python 腳本、進入互動測試環境，或以模組方式啟動內建工具時。
- **語法**：`python3 [-m] [檔案名]`
- **參數說明**：

| 參數 | 說明 |
| :--- | :--- |
| `-m` | (module) 將已安裝的套件當作指令模組來執行。 |

- **回傳值/輸出效果**：執行 Python 程式。

```shell
...
# 1. 執行特定腳本
python3 main.py

# 2. 啟動內建的簡易 HTTP 測試伺服器
python3 -m http.server 8000

# 3. 建立虛擬環境 (Virtual Environment)
python3 -m venv .venv
...
```

> **💡 小提醒**：如果你使用的是 Astral 開發的極速工具 `uv`，推薦以 `uv run main.py` 取代傳統的 `python3 main.py`，它能自動幫你處理與隔離虛擬環境，連 `source .venv/bin/activate` 都省了！

---

##### brew macOS 封包管理工具

- **使用時機**：當你想在 Mac 上安裝第三方開發工具（如 Node.js, git, htop, 甚至是 Google Chrome）時，它是 macOS 的靈魂指令。
- **語法**：`brew [install|uninstall|update] 套件名`
- **參數說明**：無特殊常用參數。
- **回傳值/輸出效果**：執行套件下載、編譯與安裝。

```shell
...
# 1. 安裝套件
brew install htop

# 2. 更新 brew 索引並升級所有已安裝套件
brew update
brew upgrade
...
```

---

##### xcode-select macOS 開發者工具鏈

- **使用時機**：買了新 Mac，剛打開終端機準備開始寫程式，發現連 `git` 或 `make` 都不能用時。
- **語法**：`xcode-select --install`
- **參數說明**：無。
- **回傳值/輸出效果**：觸發 macOS 系統彈跳視窗，下載基礎的編譯工具包。

---

##### which 尋找指令執行檔路徑

- **使用時機**：當系統報錯說找不到指令，或你想確認現在執行的 `python3` 到底是系統自帶的，還是你剛剛裝在虛擬環境裡的版本時。
- **語法**：`which 指令名稱`
- **參數說明**：無。
- **回傳值/輸出效果**：印出該指令背後真正的實體執行檔絕對路徑。

```shell
...
# 1. 查詢 python3 的確切位置
which python3
...
```

---